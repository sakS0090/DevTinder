-----currently reading about authentication-------
Cookie syntax:

res.cookie(name, value, [options]);
Datatype of each parameter:
1. name(String)
Cookie name must always be a string

2. value(string,object,buffer)